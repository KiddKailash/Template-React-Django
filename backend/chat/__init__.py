"""Chat app — a thin synchronous chat surface over the LLM harness.

Backend-only in this template: exposes `/api/chat/today/` (list) and
`/api/chat/` (send). Wire your own frontend when you're ready.
"""
